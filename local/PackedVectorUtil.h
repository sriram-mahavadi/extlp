/* 
 * File:   PackedVectorUtil.h
 * Author: sriram
 *
 * Created on 5 April, 2014, 12:43 PM
 */

#ifndef PACKEDVECTORUTIL_H
#define	PACKEDVECTORUTIL_H

#include "deprecated/PackedVectorPrototype.h"
class PackedVectorUtil {
    
//    void multiply(PackedVectorPrototypPackedVectorPrototypPackedVectorPrototypevct2, PackedVector &vctResult) {
//        // Make sure that the first vector is the one which is unpacked
//        if (!vct1.isPacked() && vct2.isPacked()){
//            multiply(vct1, vct2, vctResult);
//            return;
//        }
//        if (!vct1.isPacked()) { // Both are unpacked vectors
//
//        } else if (!vct2.isPacked()) { // vct1 is packed and vct2 is unpacked
//
//        } else { // Both vct1 and vct2 are packed vectors
//            // Make sure that vct1 has maximum number of non zeros
//            // since vct1 will get scattered
//            if(vct1.getUnPackedSize()<vct2.getUnPackedSize()){
//                multiply(vct2, vct1, vctResult);
//                return;
//            }
//       PackedVectorPrototyPackedVectorPrototyPackedVectorPrototypedVector vct1, PackedVector vct2, PackedVector &vctResult) {
//        // Make sure that the first vector is the one which is unpacked
//        if (!vct1.isPacked() && vct2.isPacked()){
//            add(vct1, vct2, vctResult);
//            return;
//        }
//        if (!vct1.isPacked()) { // Both are unpacked vectors
//
//        } else if (!vct2.isPacked()) { // vct1 is packed and vct2 is unpacked
//
//        } else { // Both vct1 and vct2 are packed vectors
//            // Make sure that vct1 has maximum number of non zeros
//            // since vct1 will get scattered
//            if(vct1.getUnPackedSize()<vct2.getUnPackedSize()){
//                multiply(vct2, vct1, vctResult);
//                return;
//            }
//        }
//    }

};

#endif	/* PACKEDVECTORUTIL_H */

